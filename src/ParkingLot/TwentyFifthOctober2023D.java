package ParkingLot;

//4. In the array below, put all the dominant elements in the array (i.e. every element that exists after it in the array) in the array into another array
//e.g.: 9 is not a dominant element in the array because 10 is bigger than 9
// 10 is a dominant element in the array because it is bigger than all the other elements after it
//Sample Input: [9, 3, 10, 7, 4]
//Sample Output: [10, 7, 4]

public class TwentyFifthOctober2023D {
    public static int[] findDominantElements(int[] input) {
        for (int index = 0; index < input.length; index++) {
            int currentElement= input[index];
            for(int index2 = 0; index2 < input.length; index2++){

            }
        }
        return  null;
    }
}