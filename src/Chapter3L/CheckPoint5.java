package Chapter3L;

import java.util.Scanner;

public class CheckPoint5 {
    public static void main(String[] args) {
        /*
        int x = 4;
        int y;
        int num1 = x;

        y = (x > 0) ? 1 : -1; //Conditional expression
        System.out.println(y);

        int num2 = y;

        int max1 = (num1 > num2) ? num1 : num2; // finding the maximum/higher number using a conditional expression
        System.out.println(max1);

         */

        Scanner input = new Scanner(System.in);
        // System.out.print("Enter a first number: ");
        // int firstNumber = input.nextInt();
        // System.out.print("Enter a second number: ");
        // int secondNumber = input.nextInt();

        // int max2 = Math.max(firstNumber, secondNumber);//finding the maximum/higher number using a Math function.
        // System.out.println(max2);


//3.34 Rewrite the following if statements using the conditional operator.
        // if (age >= 16)
        //   ticketPrice = 20;
        //else
        //  ticketPrice = 10;

        // System.out.print("Enter an age: ");
        // int age = input.nextInt();
        //int ticketPrice = (age >= 16) ? 20 : 10;
        //System.out.print("Ticket price is " + ticketPrice + ".");


        //3.35 Rewrite the following conditional expressions using if-else statements.
        //    a. score = (x > 10) ? 3 * scale : 4 * scale;
        //  b. tax = (income > 10000) ? income * 0.2 : income * 0.17 + 1000;
        //  c. System.out.println((number % 3 == 0) ? i : j);

        //a. if (score == x > 10) {
        // System.out.println(3 * scale);
        //    }
        //   else {
        // System.out.println(4 * scale);
        //    }

        //b.  if (tax == income > 10000) {
        // System.out.println(income * 0.2);
        //    }
        //   else {
        // System.out.println(income * (0.17 + 1000));
        //    }

        //c. if (number % 3 == 0) {
        // System.out.println(i);
        //   else {
        // System.out.println(j);

        // Check out Check point 3.36: Write conditional expression that returns -1 or 1 randomly.
        // Check out Check point 3.40: Is (x > 0 && x < 10) the same as ((x > 0) && (x < 10))? Is (x > 0 ||
        //x < 10) the same as ((x > 0) || (x < 10))? Is (x > 0 || x < 10 && y
        //< 0) the same as (x > 0 || (x < 10 && y < 0))?


    }
}
